<p style="color: green"><%= notice %></p>
<h1>Books</h1>

<h1>This is the authenticated version 2</h1>


<h1>Youre logged in! Welcome to the Dashboard!</h1>

<%= link_to "Sign Out", destroy_admin_session_path %> 
<h1>User books</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Book title</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>

  <% @user_books.each do |user_book| %>
    <tr>
      <td><%= user_book.user&.username || "Unknown User" %> </td>
      <td><%= user_book.book&.title || "Unknown Book" %></td>
      <td><%= link_to 'Show', user_book %></td>
      <td><%= link_to 'Edit', edit_user_book_path(user_book) %></td>
      <td><%= link_to 'Destroy', user_book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <%end%>
</tbody>
</table>

<br>
<%= link_to "New user book", new_user_book_path %>
<br>
<%= link_to 'Book index', books_path %>
<br>
<%= link_to 'User index', users_path %> 
